{
  "name": "cart-checkout-tracker",
  "version": "1.0.0",
  "description": "A simple web interface for tracking cart checkouts with 30-minute alerts",
  "main": "index.html",
  "scripts": {
    "predev": "node scripts/generate-firebase-config.mjs",
    "dev": "live-server --port=3000 --host=localhost --open=/",
    "prestart": "node scripts/generate-firebase-config.mjs",
    "start": "live-server --port=3000 --host=localhost",
    "build": "node scripts/generate-firebase-config.mjs",
    "preview": "live-server --port=4173 --host=localhost"
  },
  "keywords": [
    "cart",
    "checkout",
    "tracker",
    "staff",
    "management"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}